// Lua script to launch on boot
boot_script = "lua/game"

// Package to load on boot
boot_package = "boot"

// Linux-only configs
linux = {
	window_width = 1280
	window_height = 720
}

// Windows-only configs
windows = {
	window_width = 1280
	window_height = 720
}
