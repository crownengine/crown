// Lua script to launch on boot
boot_script = "core/game/main"

// Package to load on boot
boot_package = "boot"

window_title = "02-animation"

// Linux-only configs
linux = {
	renderer = {
		resolution = [ 1280 720 ]
		vsync = true
		fullscreen = false
		aspect_ratio = 2.5
	}
}

// Windows-only configs
windows = {
	renderer = {
		resolution = [ 1280 720 ]
		vsync = true
		aspect_ratio = 2.5
	}
}
